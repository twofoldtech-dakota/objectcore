---
"knowledge-base": patch
---

`curating-knowledge` skill and `/remember` command rewritten for the lifecycle/search/dedup/usage era: search-first before writing (`bun run kb:search`), the `kb:curate` update/supersede/deprecate/verify lifecycle (active-only INDEX = bounded forgetting), the write-time dedup refusal + `--force`, and the `kb:stats`/`kb:verify`/`kb:cite` curation and usage/ROI runbook.
