{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "test": { "dependsOn": ["^build"] },
    "build:marketplace": { "outputs": [".claude-plugin/marketplace.json"] },
    "eval": { "cache": false }
  }
}
